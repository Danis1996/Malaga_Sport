<%= form_with(model: recrutement, local: true) do |form| %>
  <% if recrutement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recrutement.errors.count, "error") %> prohibited this recrutement from being saved:</h2>

      <ul>
        <% recrutement.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <br><br><br>
      <%= form_tag url_for(action: 'create'), method: "recrutement" do %>
    <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >
        <h1 class="h3 mb-3 font-weight-normal text-center">Créer un recrutement</h1>
        <div class="form-group">
          <%= label_tag :content %>
          <%= text_area_tag :content, @recrutement.content, class: "form-control"  %>
        </div>
        <div class="form-group">
          <%= label_tag :deadline %>
          <%= date_field_tag :deadline, @recrutement.deadline %>
        </div>
        <div class="field">
        <input type="hidden" name="club_id" value="<%=@club.id%>">
        </div>

        <div class="actions mt-5">
          <%= submit_tag "Créer un recrutement", class: "btn btn-lg btn-primary btn-block" %>
        </div>
      <% end %>
      <% end %>
    </div>
  </div>
</div>