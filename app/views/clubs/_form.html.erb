<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
<%= form_with(model: club, multipart: true, local: true) do |form| %>
  <% if club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
        <% club.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<br>
<br>
<br>
<br>

<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <br><br><br>
      <%= form_tag url_for(action: 'create') do %>
      <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >
        <h1 class="h3 mb-3 font-weight-normal text-center">Créer un club</h1>
        <div class="form-group">
          <%= label_tag :Nom %>
          <%= text_field_tag :name, @club.name, autofocus: true, class: "form-control"%>
        </div>
        <div class="form-group">
          <%= label_tag :Catégorie %>
          <%= text_field_tag :category, @club.category, class: "form-control"%>
        </div>
        <div class="form-group">
          <label>Quartier</label>
          <%=select_tag 'quarter_id', options_for_select(@quarters.collect{ |t| [t.name, t.id] })%>
        </div>

        <div>
            <%= file_field_tag :picture, class: "btn btn-lg btn-primary btn-block" %>
            
          
        </div>


        <div class="actions mt-5">
          <%= submit_tag "Créer un club", class: "btn btn-lg btn-primary btn-block" %>
        </div>
      <% end %>
      <% end %>
    </div>
  </div>
</div>