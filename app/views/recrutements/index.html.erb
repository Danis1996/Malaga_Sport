<p id="notice"><%= notice %></p>

<h1>Recrutements</h1>

<table class="table">
  <thead class="thead-dark">
    <tr >
      <th scope="col" style="text-align:center;">Contenu</th>
      <th scope="col" style="text-align:center;">Date</th>
      <th colspan="3" scope="col" style="text-align:center;"></th>
    </tr>
  </thead>

  <tbody>
    <% @recrutements.each do |recrutement| %>
      <tr>
        <td style="text-align:center;"><%= recrutement.content %></td>
        <td style="text-align:center;"><%= recrutement.deadline %></td>
        <td style="text-align:center;"><%= link_to 'Afficher', recrutement %></td>

<% if current_user == recrutement.club.admin %>
        <td style="text-align:center;"><%= link_to 'Edité', edit_recrutement_path(recrutement) %></td>
        <td style="text-align:center;"><%= link_to 'Supprimer', recrutement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
<% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

